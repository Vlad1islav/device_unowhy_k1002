name: Build OrangeFox

on:
  workflow_dispatch:

jobs:
  build:
    name: Build Recovery
    runs-on: ubuntu-22.04 # Твой выбор

    steps:
    - name: Checkout Device Tree
      uses: actions/checkout@v4
      with:
        path: device_tree

    - name: Set up Build Environment
      run: |
        sudo apt-get update
        # Ставим доступные пакеты
        sudo apt-get install -y git-core gnupg flex bison build-essential zip curl zlib1g-dev gcc-multilib g++-multilib libc6-dev-i386 x11proto-core-dev libx11-dev lib32z1-dev libgl1-mesa-dev libxml2-utils xsltproc unzip fontconfig python2
        
        # Решаем проблему с libncurses5 (скачиваем вручную для 22.04)
        wget http://archive.ubuntu.com/ubuntu/pool/universe/n/ncurses/libtinfo5_6.3-2_amd64.deb
        sudo dpkg -i libtinfo5_6.3-2_amd64.deb
        wget http://archive.ubuntu.com/ubuntu/pool/universe/n/ncurses/libncurses5_6.3-2_amd64.deb
        sudo dpkg -i libncurses5_6.3-2_amd64.deb
        
        # Решаем проблему с python-is-python2
        sudo ln -sf /usr/bin/python2 /usr/bin/python

    - name: Download OrangeFox Source (Android 9.0)
      run: |
        rm -rf ~/fox_sync
        mkdir -p ~/fox_sync
        cd ~/fox_sync
        
        git config --global user.name "Builder"
        git config --global user.email "builder@example.com"
        
        curl https://storage.googleapis.com/git-repo-downloads/repo > ~/repo
        chmod a+x ~/repo
        sudo mv ~/repo /usr/local/bin/repo
        
        # Исправленный init (одна черточка -u и защита от ошибки 500)
        repo init -u https://github.com/OrangeFoxRecovery/Manifest.git -b fox_9.0 --depth=1 --no-clone-bundle
        repo sync -c -j$(nproc --all) --force-sync --no-clone-bundle --no-tags

    - name: Move Device Tree to Source
      run: |
        # Используем пути под твое дерево
        mkdir -p ~/fox_sync/device/unowhy
        mv $GITHUB_WORKSPACE/device_tree ~/fox_sync/device/unowhy/k1002

    - name: Build OrangeFox
      run: |
        cd ~/fox_sync
        source build/envsetup.sh
        export ALLOW_MISSING_DEPENDENCIES=true
        export LC_ALL="C"
        
        lunch omni_k1002-eng
        mka recoveryimage

    - name: Upload Recovery
      uses: actions/upload-artifact@v4 # Обновил до v4, чтобы не было ошибки deprecation
      with:
        name: OrangeFox-k1002-Recovery
        path: ~/fox_sync/out/target/product/k1002/recovery.img
